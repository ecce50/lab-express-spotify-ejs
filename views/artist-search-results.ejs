<h1>Search results</h1>

<% if result.length === 0{ %>
<p>No artists found</p>
<%  } else { %> 
<% result.forEach((artist) => { %>
<div>
  <img src="<%-images[].url%>" alt="Album art">
  <h4><%- artist.name%></h4>
  <button>View albums</button>

</div>
<% }) %>
<% } %>

